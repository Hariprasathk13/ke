name: tst
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

# Add dependencies under "dependencies" section, not "flutter".
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.4
  flutter_launcher_icons: ^0.14.1
flutter_icons:
  image_path: "assets/icon/app_icon.png"
  android: true
  ios: true
  # Add any other dependencies you need here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/admin_credentials.txt